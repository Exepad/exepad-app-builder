// src/app_runtime/interfaces/repo.ts
// Repository configuration for custom code files (Tier 2)
//
// Canonical types are defined in @exepad/types (packages/types/src/config.ts).
// This file re-exports them for use within the runtime package.

export type {
  RepoProps,
  RepoComponentProps,
  RepoStyleProps,
  CustomMethodProps,
  MethodType,
  SeedRepoProps,
  ImageAssetRepoProps,
} from '@exepad/types';
