# =============================================================================
# Production Dependencies for Exepad Agent
# =============================================================================
# Direct dependencies only — pip resolves transitive deps.
# Dev/test deps live in requirements-dev.txt.

# -----------------------------------------------------------------------------
# Google ADK + GCP
# -----------------------------------------------------------------------------
# Deliberately NO `[extensions]` extra: it drags in ~250 MB the agent never
# imports (kubernetes, pandas, numpy, llama-index, langgraph, docker, nltk, …
# verified: zero direct imports in apps/agent). The one extensions feature we
# use — running any LLM vendor via EXEPAD_LLM_PROVIDER (config.get_agent_model()
# → google.adk.models.lite_llm) — needs only litellm, pinned explicitly below.
google-adk==2.5.0
google-cloud-aiplatform==1.162.0
google-cloud-logging==3.16.1
google-genai==2.14.0
litellm==1.93.0

# -----------------------------------------------------------------------------
# Web framework
# -----------------------------------------------------------------------------
fastapi==0.140.7
uvicorn==0.51.0

# -----------------------------------------------------------------------------
# HTTP clients
# -----------------------------------------------------------------------------
httpx==0.28.1
aiohttp==3.14.3

# -----------------------------------------------------------------------------
# Data validation
# -----------------------------------------------------------------------------
pydantic==2.13.4
pydantic-settings==2.14.2
json_repair==0.61.7

# -----------------------------------------------------------------------------
# Database / session storage
# -----------------------------------------------------------------------------
SQLAlchemy==2.0.51
aiosqlite==0.22.1
asyncpg==0.31.0

# -----------------------------------------------------------------------------
# Parsing (validation pipeline + DesignImporter HTML tools)
# -----------------------------------------------------------------------------
sqlparse==0.5.5
tinycss2==1.5.1
tree-sitter==0.26.0
tree-sitter-typescript==0.23.2
beautifulsoup4==4.15.0

# -----------------------------------------------------------------------------
# Config & logging
# -----------------------------------------------------------------------------
python-dotenv==1.2.2
structlog==25.5.0

# -----------------------------------------------------------------------------
# MCP support for generated apps
# -----------------------------------------------------------------------------
mcp==1.28.1
