// Already-clean component: imports complete, names unique, no third-
// party imports, no app-store destructure, export name matches.
import { Button, React } from "@exepad/sdk";

export default function Cta() {
  return <Button>Continue</Button>;
}
