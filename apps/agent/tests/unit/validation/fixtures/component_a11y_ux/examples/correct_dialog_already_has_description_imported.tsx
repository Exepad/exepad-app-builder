import { Dialog, DialogContent, DialogDescription, DialogTitle } from "@exepad/sdk";

export default function CompleteDialog() {
  return (
    <Dialog>
      <DialogContent>
        <DialogTitle>Confirm</DialogTitle>
        <DialogDescription>Description already in place.</DialogDescription>
      </DialogContent>
    </Dialog>
  );
}
