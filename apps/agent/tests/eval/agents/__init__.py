"""Agent wrapper modules for ADK evaluation.

Each module in this package exposes a single agent as `agent`
for use with ADK's AgentEvaluator. This allows testing individual
subagents in isolation.
"""
