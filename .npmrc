# This repo uses pnpm (see `packageManager` in package.json). pnpm ignores npm's
# `legacy-peer-deps` flag, so it was removed to avoid silently masking real
# peer-dependency conflicts for anyone who runs `npm` out of habit. Add
# pnpm-native settings here (e.g. `strict-peer-dependencies=true`) if desired.
