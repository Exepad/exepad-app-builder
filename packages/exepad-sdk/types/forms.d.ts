// Type surface for `@exepad/sdk/forms`. Re-exports the barrel by its package
// specifier so the barrel's types (src/index.ts via the exports map, or the
// agent's flat staged gate d.ts) satisfy this subpath. See core.d.ts for the
// full rationale and the follow-on notes.
export * from '@exepad/sdk';
